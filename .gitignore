node_modules
dist
.DS_Store
*.local

# Secrets — never commit .env files at any depth. .env.example is fine.
.env
.env.*
!.env.example

.vite
server/node_modules
server/audio-cache/*
!server/audio-cache/.gitkeep
server/audio-secure/*
!server/audio-secure/.gitkeep

# TypeScript incremental build cache
*.tsbuildinfo

# Playwright MCP scratch (screenshots, console logs from automated tests)
.playwright-mcp/

# Sub-agent verification screenshots from Sonnet
gate*.png

# Claude Code per-user state (permissions, env). Skills are still tracked.
.claude/settings.local.json
.gstack/

# RVC voice-clone runtime data + artifacts (source lives in server/rvc-runner/)
server/voice-models/
server/voice-samples/
server/voice-train-logs/
server/voice-demo-backing/*.wav
server/voice-demo-backing/*.mp3
voice/
